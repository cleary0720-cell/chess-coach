name = "chess-claude-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

[vars]
# Update this to your GitHub Pages URL after deploying
ALLOWED_ORIGIN = "https://YOUR_GITHUB_USERNAME.github.io"

# Store your API key as a secret — never commit it:
# wrangler secret put ANTHROPIC_API_KEY
